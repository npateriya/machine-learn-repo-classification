# Cisco-Basic-Config-Generator
That is a Python code to convert a full cisco router or switch into a basic file, this is useful for sending config file to FE (field engineer) for replacing devices, so, the sensitive lines from the full config (SNMP, Passwords, etc) are removed.

