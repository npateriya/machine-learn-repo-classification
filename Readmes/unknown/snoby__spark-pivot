# spark-pivot

This project, in it's current form, takes JSON passed to it, formats it a bit and then sends it to a spark room.

This program along with almir/webhook (which receives webhooks) can help redirect webhook messages from alert manager to any spark room that you want.


Redirects alerts from alert manager to a cisco spark room
