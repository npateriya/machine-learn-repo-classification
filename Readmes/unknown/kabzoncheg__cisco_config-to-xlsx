# cisco_config-to-xlsx
Parses 'show interfaces', 'show version' and 'show run' Cisco IOS output and stores it in .xlsx file.
configtoxlv2.py offers extended functionality, such as arp-table parsing (with vendor lookups),
but requires "show tech" as input.
