# guest_net_auth
CLI-authentication for the Cisco authentication portal. Currently in use at the TU Berlin for authentication of guests.

# Usage
0 Arguments:
  Be promted for username/password
1 Argument:
  Path to password file (username\npassword)
2 Arguments:
  -q or -v (loglevel: quiet or verbose, logging.ERROR or logging.DEBUG)
