Cisco config to json and saved to Redis!
===================

To run the script you have to have a Redis server running on:

 redis.createClient(6379, '127.0.0.1', {});

To execute type this in the working directory:

node drive [PATH TO DIRECTORY WITH FILES]