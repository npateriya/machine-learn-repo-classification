API
==
ciscostudy.org web-service api

POST /auth
--

Authenticate with the web-service, returns an authToken
#####required parameters:
```sh
username
password
```
Example:
```sh
{ username: foo,
    password: bar }
```
POST /register
----
Register an account
#####required parameters:
```javascript
username
password
```
GET /certs
----
Returns available certs
GET /topics/&lt;*cert*&gt;
----
Returns available topics for specified cert

GET /quiz/&lt;*cert*&gt;/random
----
Returns a random quiz entry for specified cert
GET /quiz/&lt;*cert*&gt;/topic/&lt;*topic*&gt;
----
Returns a quiz entry for a specified cert and topic

