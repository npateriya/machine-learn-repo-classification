# Cisco config manager (I need to think of a real name)

### pull_config.py
This script pulls the current config from a router / switch and pushes it to the current_config branch of this repo.
Inputs needed:

1. hostname / IP address
2. username
3. password

### apply_config.py
This script applies a new config to a router / switch.
Inputs needed:

1. hostname / IP address
2. username
3. password
4. new config filename
