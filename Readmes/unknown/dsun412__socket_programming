socket_programming
==================

Implement Cisco's Optimized Edge Routing Protocol using Java Datagrams (UDP)

OER:  The idea is that some user has two paths to connect to the internet and the protocol selects the path that seems to be better, probably because it happens to be less congested.