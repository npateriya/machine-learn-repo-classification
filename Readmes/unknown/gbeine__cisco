# cisco
Cisco VPN client tools

Requires libgcrypt version 1.1.90 or newer
Compile with:

```
gcc -Wall -o cisco-decrypt cisco-decrypt.c $(libgcrypt-config --libs --cflags)
```

Usage:

```
./cisco-decrypt DEADBEEF...012345678 424242...7261
```
